- local transcription engine that allows user to hold 'fn' button and talk, and when let go, transcribed text goes text input where the cursor is

emphasis on:
- speed (spend lots of time optimizing, parallel and async operations, etc.)
- quality should be understandable
- should be easy to use
- work on macos